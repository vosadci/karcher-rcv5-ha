# CODEOWNERS for karcher_home_robots
#
# Note: this is a single-maintainer project. Per-path CODEOWNERS here
# are aspirational — they buy nothing operationally until a second
# reviewer exists. They stay in place so that (a) the audit trail is
# honest about who is nominally responsible, and (b) onboarding a
# co-maintainer is a PR rather than a redesign.
#
# Add a co-maintainer handle alongside `@vosadci` if/when one joins.
# Most-general → most-specific; the last matching rule wins.

* @vosadci

# Spec set and ADRs: changes need architectural review.
/spec/                           @vosadci
/adr/                            @vosadci

# Adapter — the one place karcher-home is imported; thin but sharp.
/custom_components/karcher_home_robots/adapter.py   @vosadci

# CI, tooling, and Claude configuration.
/.github/                        @vosadci
/.claude/                        @vosadci
/Makefile                        @vosadci
/pyproject.toml                  @vosadci
/.pre-commit-config.yaml         @vosadci
/tests/tools/                    @vosadci
